# Learnings

- This repo benefits from a local agent coordination folder even when it is intentionally excluded from git.
- Public-repo cleanup should not remove local workflow scaffolding that agents rely on unless the user asks for that tradeoff explicitly.

